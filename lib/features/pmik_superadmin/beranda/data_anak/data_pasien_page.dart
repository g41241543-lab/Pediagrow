import 'package:flutter/material.dart';

/// Halaman Data Pasien Superadmin — stub, implementasi menyusul.
class DataPasienPage extends StatelessWidget {
  const DataPasienPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Data Pasien')),
      body: const Center(child: Text('Data Pasien')),
    );
  }
}

