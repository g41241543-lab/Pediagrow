import 'package:flutter/material.dart';

/// Halaman Grafik Pengguna — stub, implementasi menyusul.
class GrafikPenggunaPage extends StatelessWidget {
  const GrafikPenggunaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Grafik Pengguna')),
      body: const Center(child: Text('Grafik Pengguna')),
    );
  }
}
