import 'package:flutter/material.dart';

/// Halaman Daftar Resep MPASI Admin — stub, implementasi menyusul.
class DaftarResepMpasiAdminPage extends StatelessWidget {
  const DaftarResepMpasiAdminPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Daftar Resep MPASI')),
      body: const Center(child: Text('Daftar Resep MPASI')),
    );
  }
}
