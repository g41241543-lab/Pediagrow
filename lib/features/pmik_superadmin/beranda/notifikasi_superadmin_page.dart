import 'package:flutter/material.dart';

/// Halaman Notifikasi Superadmin — stub, implementasi menyusul.
class NotifikasiSuperadminPage extends StatelessWidget {
  const NotifikasiSuperadminPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Notifikasi')),
      body: const Center(child: Text('Notifikasi Superadmin')),
    );
  }
}
