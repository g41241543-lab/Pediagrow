import 'package:flutter/material.dart';

/// Halaman Daftar Soal Permainan Admin — stub, implementasi menyusul.
class DaftarSoalPermainanPage extends StatelessWidget {
  const DaftarSoalPermainanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Permainan')),
      body: const Center(child: Text('Daftar Soal Permainan')),
    );
  }
}
