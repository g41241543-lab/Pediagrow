import 'package:flutter/material.dart';

/// Halaman Rekapitulasi Stunting Superadmin — stub, implementasi menyusul.
class RekapitulasiStuntingPage extends StatelessWidget {
  const RekapitulasiStuntingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Rekapitulasi Stunting')),
      body: const Center(child: Text('Rekapitulasi Stunting')),
    );
  }
}
