import 'package:flutter/material.dart';

/// Halaman Konsultasi Superadmin — stub, implementasi menyusul.
class KonsultasiSuperadminPage extends StatelessWidget {
  const KonsultasiSuperadminPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Konsultasi')),
      body: const Center(child: Text('Konsultasi Superadmin')),
    );
  }
}
