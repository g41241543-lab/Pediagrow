import 'package:flutter/material.dart';

/// Halaman Daftar Riwayat Konsultasi Admin — stub, implementasi menyusul.
class DaftarRiwayatKonsultasiAdminPage extends StatelessWidget {
  const DaftarRiwayatKonsultasiAdminPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Riwayat Konsultasi')),
      body: const Center(child: Text('Daftar Riwayat Konsultasi')),
    );
  }
}
